<h1>Alumnos</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Boleta</th>
      <th>CVU</th>
      <th>Nombre(s)</th>
      <th>Primer Apellido</th>
      <th>Segundo Apellido</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @alumnos.each do |alumno| %>
      <tr>
        <td><%= alumno.boleta %></td>
        <td><%= alumno.cvu %></td>
        <td><%= alumno.user.nombre %></td>
        <td><%= alumno.user.primer_apellido %></td>
        <td><%= alumno.user.segundo_apellido %></td>
        <td><%= link_to 'Show', alumno %></td>
        <td><%= link_to 'Edit', edit_alumno_path(alumno) %></td>
        <td><%= link_to 'Destroy', alumno, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Alumno', new_alumno_path %>
