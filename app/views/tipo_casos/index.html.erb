<p id="notice"><%= notice %></p>

<h1>Tipo Casos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_casos.each do |tipo_caso| %>
      <tr>
        <td><%= tipo_caso.nombre %></td>
        <td><%= link_to 'Show', tipo_caso %></td>
        <td><%= link_to 'Edit', edit_tipo_caso_path(tipo_caso) %></td>
        <td><%= link_to 'Destroy', tipo_caso, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo Caso', new_tipo_caso_path %>
