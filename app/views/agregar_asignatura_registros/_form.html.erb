<%= form_with(model: agregar_asignatura_registro, local: true) do |form| %>
  <% if agregar_asignatura_registro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agregar_asignatura_registro.errors.count, "error") %> prohibited this agregar_asignatura_registro from being saved:</h2>

      <ul>
        <% agregar_asignatura_registro.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :asignatura_registro_id %>
    <%= form.text_field :asignatura_registro_id %>
  </div>

  <div class="field">
    <%= form.label :caso_id %>
    <%= form.text_field :caso_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
