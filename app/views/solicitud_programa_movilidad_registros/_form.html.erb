<%= form_with(model: solicitud_programa_movilidad_registro, local: true) do |form| %>
  <% if solicitud_programa_movilidad_registro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(solicitud_programa_movilidad_registro.errors.count, "error") %> prohibited this solicitud_programa_movilidad_registro from being saved:</h2>

      <ul>
        <% solicitud_programa_movilidad_registro.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :nombre_institucion %>
    <%= form.text_field :nombre_institucion %>
  </div>

  <div class="field">
    <%= form.label :caso_id %>
    <%= form.text_field :caso_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
